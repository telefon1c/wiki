apiVersion: apps/v1
kind: Deployment
metadata:
  name: docs-v4
  namespace: docs-telefon1c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docs-v4
  template:
    metadata:
      labels:
        app: docs-v4
    spec:
      containers:
        - name: nginx
          image: ghcr.io/telefon1c/docs-v4:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
